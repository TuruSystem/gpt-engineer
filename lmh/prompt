create a python based multi os application that can run on windows, macos, and linux. where the model will be dwnloaded from hugginface and get self hosted on user machine and a web based user chat interface too should get with it. a ull bundle.